import initialState from './initialState'

const ChordListReducer = (state = initialState.chordlist, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default ChordListReducer