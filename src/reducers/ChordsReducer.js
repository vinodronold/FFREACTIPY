import initialState from './initialState'

const ChordsReducer = (state = initialState.chords, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default ChordsReducer